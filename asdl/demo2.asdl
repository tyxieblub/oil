module arith {
  -- Simple Sum Type
  op_id = Plus | Minus | Star

  -- Compound Sum Type
  cflow =
    Break
  | Continue
  | Return(int status)

  source_location = (string path, int line, int col, int length)
}
